schema: spec-driven

context: |
  Project: CDC Prior Work Assertions — re-analysis of CDC-25 codebase
  Tech stack: Python 3.11+, EnergyPlus FMU, PyFMI, CVXPY, TenSEAL (BFV), NumPy, Pandas, Matplotlib
  Conda env: ccm
  Goal: Four-way controller comparison (rule-based, uncoor MPC, plaintext ADMM, encrypted ADMM)
  Deliverables: Publication-quality figures + tables for NSF EPCN proposal §3.1
  Key constraint: Original codebase in original_codebase/ is read-only; modifications go in src/
rules:
  proposal:
    - Reference the phase (0-4) from prior_work_plan_CDC.md
    - Every assertion must trace to a verification check in prior_work_verify_CDC.md
  specs:
    - Include expected numerical values from the published CDC paper
  design:
    - Respect the read-only original codebase constraint
  tasks:
    - Map each task to verification check tags (R, C, A)